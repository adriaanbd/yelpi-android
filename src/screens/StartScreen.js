import React from 'react';
import {View} from 'react-native';
import {Text} from 'react-native-elements';

const StartScreen = () => (
  <View>
    <Text>HELP START SCREEN</Text>
  </View>
);

export default StartScreen;
